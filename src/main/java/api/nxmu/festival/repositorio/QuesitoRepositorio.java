package api.nxmu.festival.repositorio;
import org.springframework.data.jpa.repository.JpaRepository;

import api.nxmu.festival.modelo.Quesito;

public interface QuesitoRepositorio extends JpaRepository<Quesito, Long>{
 
}
