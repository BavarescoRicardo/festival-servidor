package api.nxmu.festival.repositorio;
import api.nxmu.festival.modelo.Jurado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JuradoRepositorio extends JpaRepository<Jurado, Long>{
 
}
